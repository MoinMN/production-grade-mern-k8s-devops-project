apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: portfolio

spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 4518
      targetPort: 4518
  type: ClusterIP
